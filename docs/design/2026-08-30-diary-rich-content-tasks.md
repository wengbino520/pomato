# 日记富文本增强开发任务清单（依赖排序 · 可断点续传）

> 创建时间: 2026-08-30  
> 总预估: 3-5 天  
> 每个 TASK 完成后应做 git commit，标题以 `TASK-NN` 结尾

---

## 第一层：数据与兼容层（零依赖基础设施）

### TASK-01: `database.py` — 扩展 diary schema，新增富文本兼容字段
- **状态**: ⬜ 未开始
- **依赖**: 无
- **文件**: `src/core/database.py`
- **操作**:
  1. 检查 `diary_entries` 表结构
  2. 增加 `content_html TEXT DEFAULT ''`
  3. 增加 `attachments_json TEXT DEFAULT '[]'`
  4. 增加 `has_rich_media INTEGER DEFAULT 0`
  5. 编写迁移逻辑，保持旧表兼容
- **验证**:
  - 读取旧日记不报错
  - 新字段存在且可写入
  - 旧字段 `content` 仍然保留
- **预计**: 45 分钟

### TASK-02: `database.py` — 读写日记时兼容新旧字段
- **状态**: ⬜ 未开始
- **依赖**: TASK-01
- **文件**: `src/core/database.py`
- **操作**:
  1. `save_diary()` 支持写入 `content_html` 与 `attachments_json`
  2. `get_diary()` 读取时优先 `content_html`，回退到 `content`
  3. 保证旧日记仍可打开
- **验证**:
  - 旧文本日记正常读取
  - 富文本日记可写回
  - 读取的字段兼容且不发生空值崩溃
- **预计**: 60 分钟

---

## 第二层：附件与文件管理

### TASK-03: 新增附件目录与保存逻辑
- **状态**: ⬜ 未开始
- **依赖**: TASK-01
- **文件**: `src/core/database.py`, `src/services/diary_service.py`（如有）
- **操作**:
  1. 设计图片附件目录：`~/.pomato/profiles/<profile>/diary_attachments/YYYY-MM-DD/`
  2. 保存图片文件时生成唯一文件名
  3. 保存 `attachments_json` 元数据
  4. 处理重复写入与覆盖逻辑
- **验证**:
  - 图片文件落盘成功
  - 文件路径能被读取
  - 资料空间隔离有效
- **预计**: 60 分钟

### TASK-04: `DiaryService` / `DiaryWidget` — 图片清理与异常处理
- **状态**: ⬜ 未开始
- **依赖**: TASK-03
- **文件**: `src/services/diary_service.py`, `src/ui/diary_widget.py`
- **操作**:
  1. 图片缺失时输出安全提示
  2. 删除日记时清理附件引用
  3. 处理图片文件损坏或无法读取
- **验证**:
  - 缺失图片不导致崩溃
  - 删除日记后不残留无效文件引用
- **预计**: 45 分钟

---

## 第三层：UI 编辑增强

### TASK-05: `diary_widget.py` — 切换富文本编辑器并支持粘贴
- **状态**: ⬜ 未开始
- **依赖**: TASK-02
- **文件**: `src/ui/diary_widget.py`
- **操作**:
  1. 将纯文本编辑区切换为 `QTextEdit` 富文本模式
  2. 开启 `setAcceptRichText(True)`
  3. 支持普通粘贴与 HTML 粘贴
  4. 对不安全 HTML 进行过滤
- **验证**:
  - 纯文本粘贴正常
  - 富文本粘贴不报错
  - 输入框无明显异常
- **预计**: 90 分钟

### TASK-06: `diary_widget.py` — 支持图片粘贴与拖拽插入
- **状态**: ⬜ 未开始
- **依赖**: TASK-03, TASK-05
- **文件**: `src/ui/diary_widget.py`
- **操作**:
  1. 监听剪贴板图片数据
  2. 读取 `QImage` / `QPixmap`
  3. 保存到附件目录并插入到文档
  4. 支持拖拽本地图片进入编辑区
- **验证**:
  - 粘贴图片正常显示
  - 拖拽图片输入成功
  - 图片保存到附件目录
- **预计**: 90 分钟

### TASK-07: `diary_widget.py` — 支持简单富文本工具栏
- **状态**: ⬜ 未开始
- **依赖**: TASK-05
- **文件**: `src/ui/diary_widget.py`
- **操作**:
  1. 增加常用操作：加粗、列表、标题、引用
  2. 增加图片插入和表格插入按钮
  3. 让用户可用工具栏而不是纯文本输入
  4. 保持最小干扰，不重构整个 UI
- **验证**:
  - 选中文本可加粗/列表化
  - 图片/表格按钮可插入内容
  - 工具栏不会遮挡日记区域
- **预计**: 75 分钟

### TASK-08: `diary_widget.py` — 支持表格插入与基本编辑
- **状态**: ⬜ 未开始
- **依赖**: TASK-05, TASK-07
- **文件**: `src/ui/diary_widget.py`
- **操作**:
  1. 增加“插入表格”动作
  2. 支持表格单元格编辑
  3. 支持插入/删除行列
  4. 支持表格内文本修改和保存
- **验证**:
  - 可插入 2x2 及更多表格
  - 可编辑单元格内容
  - 表格保存后重新打开仍能显示
- **预计**: 120 分钟

---

## 第四层：历史查看增强

### TASK-09: `diary_history_window.py` — 回看富文本内容
- **状态**: ⬜ 未开始
- **依赖**: TASK-02, TASK-05
- **文件**: `src/ui/diary_history_window.py`
- **操作**:
  1. 读取 `content_html` 并渲染
  2. 若为空则回退 `content`
  3. 显示图片/表格等富内容
- **验证**:
  - 历史日记中的表格图片可展示
  - 旧日记不报错
- **预计**: 75 分钟

### TASK-10: `diary_history_window.py` — 附件/图片列表展示
- **状态**: ⬜ 未开始
- **依赖**: TASK-09
- **文件**: `src/ui/diary_history_window.py`
- **操作**:
  1. 在日记历史中展示附件列表
  2. 点击附件可打开/预览
  3. 支持图片缩略显示
- **验证**:
  - 历史中可看到图片缩略
  - 点击可预览
- **预计**: 60 分钟

---

## 第五层：测试与回归

### TASK-11: `tests/` — 新增富文本日记测试
- **状态**: ⬜ 未开始
- **依赖**: TASK-01 ~ TASK-10
- **文件**: `tests/test_diary_service.py`, `tests/test_diary_widget.py`, `tests/test_diary_history_window.py`
- **操作**:
  1. 测试旧文本日记兼容
  2. 测试富文本保存
  3. 测试图片附件保存
  4. 测试历史查看富文本回显
- **验证**:
  - 全量 `pytest tests/` 通过
  - 新增测试覆盖边界场景
- **预计**: 90 分钟

### TASK-12: 回归检查与 UI sanity
- **状态**: ⬜ 未开始
- **依赖**: TASK-11
- **文件**: 所有相关 UI 文件
- **操作**:
  1. 日记主窗口验证无布局破坏
  2. 历史窗口与主窗口切换正常
  3. 资料空间隔离验证
- **验证**:
  - UI 无明显崩溃
  - 无回归到旧的纯文本模式
- **预计**: 45 分钟

---

## 检查点（CP）

| 检查点 | 完成条件 | 关联 TASK |
|--------|----------|-----------|
| CP-1 | 数据兼容与附件保存方案确认 | TASK-01 ~ TASK-04 |
| CP-2 | 富文本编辑器与图片插入可用 | TASK-05 ~ TASK-08 |
| CP-3 | 历史回看与富内容展示可用 | TASK-09 ~ TASK-10 |
| CP-4 | 测试与回归完全通过 | TASK-11 ~ TASK-12 |

---

## 关键验收门禁

在实现前，必须满足：

- [ ] 旧日记数据兼容方案已确认
- [ ] 富文本 / 图片的落盘方式已确认
- [ ] 资料空间隔离策略已纳入设计
- [ ] UI 改造范围已控制在日记模块内
